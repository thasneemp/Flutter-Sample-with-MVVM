import 'package:flutter/material.dart';
import 'package:mobx/mobx.dart';

abstract class BaseStateFullWidget<viewState extends Store> extends StatefulWidget {
  const BaseStateFullWidget({Key? key}) : super(key: key);

}
